# Directus Receipt Autofill

Custom Directus interface to get a receipt template and fill a rich text field

# Compiling

You can run `yarn bundle` to watch file changes and bundle the `index.js` with rollup
